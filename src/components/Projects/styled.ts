import styled from "styled-components"

export const ListItem = styled.div`
        max-width: 1200px;
        margin: 0 auto;
        padding: 16px 24px;

        position: relative;

        display: flex;
        justify-content: flex-start;
        align-items: center;

        padding-top: 64px;
        padding-bottom: 64px;
`;